# --- API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20

# --- Base relationnelle ---
sqlalchemy==2.0.36
psycopg[binary]==3.2.3

# --- Authentification (JWT) ---
pyjwt==2.10.1
bcrypt==4.2.1

# --- RAG / LLM ---
langchain==0.3.14
langchain-community==0.3.14
langchain-text-splitters==0.3.4
langchain-core==0.3.29
langchain-ollama==0.2.2
langgraph==0.2.62
qdrant-client==1.12.2

# --- Embeddings BGE-M3 ---
FlagEmbedding==1.3.3
sentence-transformers==3.3.1

# --- Extraction PDF ---
PyMuPDF==1.25.1

# --- Tracing / observabilité (SDK léger, utilisé au runtime si configuré) ---
langfuse==2.57.0

# NB : torch (CPU) est installé séparément dans le Dockerfile, depuis l'index
# CPU de PyTorch, pour éviter le téléchargement des librairies CUDA inutiles.
#
# DeepEval et Langfuse ne sont PAS des dépendances du runtime de l'API : ils
# servent à l'évaluation / au tracing hors ligne. Voir requirements-eval.txt.
